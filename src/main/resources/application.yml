spring:
  profiles:
    active: local
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true